# 3D Anime 使い方ガイド

このアプリは、3Dキャラクターを表示して、ダンスアニメーションを再生できるWebアプリです。
スマホ・タブレット・PCで利用できます。

## 1. まずは準備

このプロジェクトを開き、依存関係をインストールします。

```bash
npm install
```

## 2. 開発サーバーを起動

```bash
npm run dev
```

その後、ブラウザで次のURLを開いてください。

```text
http://localhost:3000
```

## 3. 画面の見方

- 左上のメニューで操作方法を確認できます
- メニューは「開く / 閉じる」で折りたたみ可能です
- 3Dモデルはドラッグで回転できます
- マウスホイールでズームできます
- 下のボタンでアニメやリセットを操作できます

## 4. 基本操作

### ダンスを再生する

1. 画面下の「ダンス再生」を押す
2. モデルがアニメーションを再生する
3. 音声付きの動画が表示される場合があります

### 画面をリセットする

- 「リセット」を押すとページを再読み込みします

### 3Dモデルの見え方を調整する

- 左クリック＋ドラッグ: 視点を回す
- ホイール: 拡大 / 縮小
- 画面の端にあるメニューを開閉して見やすくする

## 5. モデルファイルがない場合

このアプリは `public/3d/` 配下の GLB モデルを読み込みます。

```text
public/3d/
├── kawaii22.glb
├── motion.glb
└── original_movie.mp4
```

もしファイルがない場合は、以下のように配置してください。

```bash
mkdir -p public/3d
```

その後、対応する GLB / MP4 ファイルを `public/3d/` に置きます。

> 3Dモデルが見えない場合は、ファイル名と保存先を確認してください。

## 6. 本番用ビルド

アプリを本番用にビルドしたい場合は次を実行します。

```bash
npm run build
```

その後、起動します。

```bash
npm run start -- --hostname 0.0.0.0 --port 3000
```

## 7. スマホ・タブレットでの使い方

- 画面を横向きにすると見やすくなります
- 左上のメニューは小さく折りたたんで使えます
- 下部の操作ボタンは画面幅に合わせて自動で収まります
- 画面の隅を押さえすぎないように、メニューは折りたたんで使うと自然です

## 8. よくある問題

### 画面が真っ黒で何も見えない

- 3Dファイルが存在しているか確認
- ブラウザが WebGL をサポートしているか確認
- ページを再読み込みする

### ダンスが再生されない

- モデルの読み込みが終わっているか確認
- しばらく待ってから「ダンス再生」を押す
- 画面の再読み込みを試す

### ボタンが押しづらい

- 左上メニューを閉じて、下の操作ボタンを使う
- スマホでは横向きで見ると見やすくなります

## 9. 参考

- 3Dモデルの作成: VRoid Studio
- ダンス動画の参考: YouTube
- 音楽: YOASOBI「アイドル」

必要なら次に、
- デザインをもっとかわいい見た目にする
- 日本語のライセンス表示を追加する
- デプロイ用の設定を整える

といった改善もできます。
